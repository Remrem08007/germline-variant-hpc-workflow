process {
    executor = 'slurm'
    resourceLimits = [cpus: 64, memory: 512.GB, time: 72.h]
}
executor {
    queueSize = 40
    pollInterval = '30 sec'
    submitRateLimit = '10/1min'
}
// No scheduler account or partition is hard-coded.
